# Supabase — Project Settings → API
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...          # server-side only, NEVER expose in client code

# Vision AI fallback (used only when a file has no embedded metadata)
ANTHROPIC_API_KEY=sk-ant-...

# Secret for POST /api/admin/add-prompt (pick any long random string)
ADMIN_SECRET=change-me-to-something-long-and-random
